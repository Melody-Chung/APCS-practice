#include <iostream>

using namespace std;

int main()
{
    int b=1;
    string a;
    cin >> a;

    for(int i=0; i<a.size(); i++){
        if(a[i]!=a[a.size()-1-i]){
            b=0;
        }
    }
    if(b==0){
        cout << "no";
    }
    else{
        cout << "yes";
    }
    return 0;
}
